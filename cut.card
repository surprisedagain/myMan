= cut =
== COMMAND FORM ==
    cut [-cfdw] list [file] ...

== EXAMPLE ==
    cut -c  -3,5,8-10,15- long_lined_file.txt

== OPTIONS ==
	-c list     # character positions
	-f list     # The list specifies fields, separated field delimiter character
	-d delim    # Use <delim> as the field delimiter instead of tab
	              consecutive <delim> make empty fields
	-w          # Use whitespace (spaces and tabs) as delimiter.  Consecutive
                  spaces and tabs count as one single field separator.
