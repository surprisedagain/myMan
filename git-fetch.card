= git-fetch =
[[git-pull]]
== COMMAND FORM ==
    git fetch

== EXAMPLES: ==
    git fetch origin main

== COMMON OPERATIONS: ==
    git fetch <REMOTE> [<BRANCH>]
                    # download REMOTE commits on BRANCH
                    # Default all REMOTE branches

git fetch updates remote tracking branches.
pulls down commits, stores them in the local repo, and updates branch pointers

? does not move HEAD
? should not modify working tree or staging area
