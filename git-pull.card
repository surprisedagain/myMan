= git pull =
[[git-fetch]]

== COMMAND FORM ==
    git pull

== EXAMPLES ==
    git pull
    git fetch origin main

Updates the current branch. git-fetch commits from remote version of current
branch; then merge those changes.

git pull does a [[git-fetch]] and then a [[git-merge]]

== COMMON OPERATIONS ==
    git pull        # pull everything merge if required
                    # almost dangerously easy with no option

